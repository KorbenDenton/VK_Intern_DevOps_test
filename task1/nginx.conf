events {}

http {
    server {
        listen 80;
        server_name localhost;
        
        location / {
            default_type text/plain;
            return 200 "Hello World\n";
        }
        
        location /health {
            return 200 "ok\n";
        }
    }
}